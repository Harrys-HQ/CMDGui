model: ollama_chat/gemma4:e4b
git: true
check-update: false
auto-commit: false
show-diffs: true
# Local models can be slow, so let's use a smaller map-tokens if needed, 
# but 1024 is usually fine for Gemma 4.
map-tokens: 1024
# Dark mode for the terminal UI if the user prefers
dark-mode: true
